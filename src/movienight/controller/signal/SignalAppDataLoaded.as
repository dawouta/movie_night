package movienight.controller.signal
{
	import org.osflash.signals.Signal;
	
	public class SignalAppDataLoaded extends Signal
	{
		public function SignalAppDataLoaded(...parameters)
		{
			super(parameters);
		}
	}
}